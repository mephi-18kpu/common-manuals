# Подготовка учебного репозитория

## 1. Общая организация

- Все учебные репозитории принадлежат организации [`mephi-18kpu`](https://github.com/mephi-18kpu), а не личным аккаунтам преподавателей.
- Для каждого курса, семестра или самостоятельного набора материалов создаётся отдельный репозиторий.
- Название формируется по шаблону `<год>-<семестр>-<краткое-название>-<назначение>`.
- В качестве назначения можно использовать `course`, `materials` или другое слово, которое точнее отражает содержимое репозитория. Предпочитайте уже используемые термины для обозначения однотипного содержимого.
- Примеры: `2026-autumn-python-course`, `2026-autumn-miluino-course`, `stm32-reference-materials`.
- Для учебных репозиториев основной веткой является `master`.
- Ветка `master` содержит общие материалы, задания, инструкции и служебные файлы. Студенческий код хранится в отдельных рабочих ветках заданий и не переносится в `master`.

## 2. Получение репозитория и доступа

- Перед началом курса преподаватель обращается к администратору организации с просьбой создать репозиторий или предоставить доступ к уже существующему.
- В запросе следует указать предлагаемое название и назначение репозитория, учебный курс и семестр, требуемую видимость, список преподавателей и учебных групп.
- Преподаватель получает роль `Maintain` от администратора организации и использует для работы личный аккаунт GitHub.
- Если преподавателю требуется изменить настройку, недоступную с ролью `Maintain`, он обращается к администратору организации.

## 3. Доступ с аудиторных компьютеров

- Для каждого сочетания аудитории и курса используется команда по шаблону `<аудитория>-<курс>-workstations`, например `a220-python-workstations`, с ролью `Write` в соответствующем репозитории.
- В команду входят выделенные аккаунты GitHub аудиторных компьютеров, а не личные аккаунты студентов. Несколько учебных групп могут использовать одну команду, если занимаются в одной аудитории в рамках одного курса.
- Преподаватель сообщает администратору, какие аудитории и курсовые учётные записи Windows должны получить доступ к репозиторию.
- После настройки доступа преподаватель проверяет с рабочих станций, что репозиторий доступен и ветки `student/**/*` можно обновлять обычным push, но защищённые `master` и `submissions/*` нельзя изменять напрямую.
- Если компьютер не видит репозиторий или имеет неправильный уровень доступа, преподаватель передаёт администратору аудиторию, номер компьютера, логин аккаунта рабочего места, команду и название репозитория.
- Личность студента определяется по ветке `student/<группа>/student-<номер-студента-в-группе>/<задание>`, директории и данным преподавателя; аккаунт GitHub определяет только рабочее место.

## 4. Проверка репозитория перед началом курса

До начала занятий преподаватель проверяет:

- название, описание и видимость репозитория;
- наличие актуального `README.md`;
- наличие ссылки на общие инструкции `common-manuals`;
- соответствие default branch принятому имени `master`;
- наличие правил именования студенческих веток и директорий;
- принято решение о ведении таблиц успеваемости в GitHub: при их использовании предусмотрено уведомление студентов, а при отказе удалена шаблонная директория `study-groups/`;
- наличие необходимых учебных материалов;
- файл `.gitignore` проверен с учётом языков, программ и создаваемых в рамках курса временных или генерируемых файлов и при необходимости дополнен либо изменён; правила не должны исключать исходные файлы и результаты, которые студенты обязаны сдавать;
- наличие активной защиты ветки `master`, веток `submissions/*` и веток `student/**/*`;
- работоспособность примеров команд и ссылок;
- доступ преподавателей и команд аудиторных компьютеров.

Если название, видимость, default branch, правила защиты или права доступа настроены неправильно, преподаватель обращается к администратору до начала работы студентов.

## 5. Проверка защиты веток

До предоставления студентам доступа преподаватель убеждается, что:

- для `master` и `submissions/*` запрещены удаление, force-push, прямой push студентов и объединение Pull Request аккаунтами рабочих станций; изменения вносятся через Pull Request, а merge выполняют преподаватели или администраторы;
- для `master` и `submissions/*` действуют предусмотренные курсом требования к согласованию изменений;
- создавать `submissions/*` могут преподаватели с ролью `Maintain` и администраторы организации;
- для `student/**/*` запрещены удаление и force-push, но разрешён обычный push аккаунтов рабочих станций;
- преподаватель может выполнять необходимые действия в пределах своей роли.

Если защита отсутствует, не применяется к нужным веткам или мешает предусмотренной работе преподавателя, следует обратиться к администратору организации.

[Вернуться к общим инструкциям](../README.md)
